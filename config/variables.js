'use strict'

// выгружаем свойства из объекта GameEngine (деструктуризация)
const { Game, Scene, Body, Controller, Point, Line } = GameEngine
const { MainController, BunnyController } = Controller